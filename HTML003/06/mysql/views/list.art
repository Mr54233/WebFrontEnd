<!DOCTYPE html>
<html lang="zh-CN">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>用户列表</title>
		<!-- 一般的 ,1. 从html静态文件改成模板文件 , 需要处理路径 -->
		<link rel="stylesheet" href="/css/bootstrap.min.css" />
	</head>
	<body>
		<table class="table table-striped">
			<tr>
				<th>编号</th>
				<th>姓名</th>
				<th>年龄</th>
				<th>性别</th>
				<th>状态</th>
			</tr>
			<!-- 2. 处理动态内容 , 换成模板语法-->
			{{each users}}
			<tr>
				<td>{{ $value.uid }}</td>
				<td>{{ $value.uname }}</td>
				<td>{{ $value.age }}</td>
				<td>{{ $value.gender }}</td>
				<td>{{ $value.state }}</td>
			</tr>
			{{/each}}
		</table>
		<div class="func">
			<a href="" class="btn btn-success">注册用户</a>
		</div>
	</body>
</html>
