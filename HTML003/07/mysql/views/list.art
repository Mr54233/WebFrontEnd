<!DOCTYPE html>
<html lang="zh-CN">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>用户列表</title>
		<!-- 一般的 ,1. 从html静态文件改成模板文件 , 需要处理路径 -->
		<link rel="stylesheet" href="/css/bootstrap.min.css" />
	</head>
	<body>
		<div style="width: 800px; margin: 10px auto">
			<table class="table table-striped">
				<tr>
					<th>编号</th>
					<th>姓名</th>
					<th>年龄</th>
					<th>性别</th>
					<th>状态</th>
					<th>操作</th>
				</tr>
				<!-- 2. 处理动态内容 , 换成模板语法-->
				{{each users}}
				<tr>
					<td>{{ $value.uid }}</td>
					<td>{{ $value.uname }}</td>
					<td>{{ $value.age }}</td>
					<td>{{ $value.gender }}</td>
					<td>{{ $value.state }}</td>
					<td>
						<a
							href="/user/edit?uid={{ $value.uid }}"
							class="btn btn-primary"
							>编辑</a
						>
						<a
							href="/user/delete?uid={{ $value.uid }}"
							class="btn btn-danger"
							>删除</a
						>
					</td>
				</tr>
				{{/each}}
			</table>

			<!-- 分页组件 -->
			<nav aria-label="Page navigation">
				<ul class="pagination">
					<li>
						{{ if (!pager.isFirstPage)}}
						<a href="/user/list?page={{ pager.prev }}" aria-label="Previous">
							<span aria-hidden="true">&laquo;</span>
						</a>
						{{ /if }}
					</li>

					{{ each pager.nums}}
					<li><a href="/user/list?page={{ $value }}">{{ $value }}</a></li>
					{{/each}}

					<li>
						{{ if (!pager.isLastPage)}}
						<a href="/user/list?page={{ pager.next }}" aria-label="Next">
							<span aria-hidden="true">&raquo;</span>
						</a>
						{{ /if }}
					</li>
				</ul>
			</nav>

			<div class="func">
				<a href="/user/reg" class="btn btn-success">注册用户</a>
			</div>
		</div>
	</body>
</html>
